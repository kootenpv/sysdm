sudo: false
env:
  - TRAVIS=true
language: python
dist: xenial

before_install: sudo apt-get install --reinstall systemd

python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"

install: pip install tox-travis
script: tox